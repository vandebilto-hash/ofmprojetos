export default function ForgotPasswordPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-canvas">
      <div className="w-full max-w-md rounded-lg border border-line bg-white p-8 shadow-soft">
        <h1 className="text-2xl font-bold">Recuperar senha</h1>
        <p className="mt-2 text-sm text-slate-600">
          Estrutura preparada para envio futuro de e-mail. No ambiente local, o administrador pode
          redefinir a senha pelo cadastro de usuarios.
        </p>
      </div>
    </main>
  );
}
